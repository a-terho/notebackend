{
  "name": "notebackend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "a-terho",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "cd ../notefrontend && npm run build:ui",
    "deploy": "npm run build:ui && git add . && git commit -m autobuild && git push",
    "deploy:ui": "npm run build:ui && git add dist && git commit -m uibuild && git push"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "mongoose": "^9.3.3"
  }
}
